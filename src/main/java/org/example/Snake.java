package org.example;

import javax.swing.*;
import java.awt.*;

public class Snake {
    public void paint(Graphics g) {
        g.setColor(Color.green);
        g.fillRect(30, 30, 100, 100);
    }
}
